{
  let foo = 'bar';
}

console.log(foo);

// what happens when you run this? why do we get this result?
// error of foo not defined because foo is only defined in the block scope not the global scope