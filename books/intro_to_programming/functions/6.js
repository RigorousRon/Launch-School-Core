/*
In the code shown below, identify the following items:

the function arguments
the function body
the function declaration
the function invocation
the function name
the function parameters
the function return value
the names of all variables in this program
*/

function multiplyNumbers(num1, num2, num3) {
  let result = num1 * num2 * num3;
  return result;
}

let product = multiplyNumbers(2, 3, 4);

/*
arguments are 2, 3, 4
function body is everything between the curly braces
function declaration is the word function followed by the function name
function invocation is on the right side of the assignment of product
function name is multiplyNumbers
function parameters are num1, num2, num3
function return value is result
variables are num1, num2, num3, result, product, multiplyNumbers

*/