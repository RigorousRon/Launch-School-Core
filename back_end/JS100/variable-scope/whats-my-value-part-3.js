// What will the following code log to the console and why? Don't run it until you have tried to answer.

if (true) {
  let myValue = 20;
}

console.log(myValue);

// This will throw an error of myValue is not defined since myValue is only accessible in its own block scope.