//Write three different ways to remove all of the elements from the following
//array:

let numbers = [1, 2, 3, 4];

// numbers.forEach(number => numbers.unshift(number)); X CHANGING ARRAY LENGTH X

// numbers = [];

// for (let index = 0; index < 4; index += 1) {
//   numbers.pop();
// }

// for (let index = 0; index < 4; index += 1) {
//   numbers.shift();
// }

// numbers.length = 0;

// numbers.splice(0, numbers.length);

// while (numbers.length) {
//  numbers.pop();
// }


console.log(numbers);